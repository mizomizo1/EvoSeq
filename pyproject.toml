[project]
name = "evoseq"
version = "0.1.0"
description = "Genome analysis toolkit powered by Evo"
requires-python = ">=3.10"

dependencies = [
    "torch",
    "transformers",
    "biopython",
    "numpy",
]